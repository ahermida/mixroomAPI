/*
   delete.go -- DB queries that delete objects
*/
package db

//[DELETE] remove a thread
func DeleteThread(threadID, userID string) {
  //call DB
}

//[DELETE] removes a post
func DeletePost(postID, userID string) {
  //call DB
}

//[DELETE] actually deletes a user (for dev use only!)
func RemoveUser(uid string) {
  //call DB
}
