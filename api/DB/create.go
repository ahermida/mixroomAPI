/*
   create.go -- DB queries that create objects
*/
package db


//[CREATE] creates user in the Database with given email, username, and hashed password
func CreateUser(email, username, password string) {
  //call DB
}

//[CREATE] creates a thread from a struct of a given JSON
func CreateThread() {
  //call DB
}

//[CREATE] creates a post for a given thread -- with given response list
func CreatePost() {
  //call DB
}
